apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-persistent-volume-claim
  namespace: resource
spec:
  storageClassName: standard-rwo
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi